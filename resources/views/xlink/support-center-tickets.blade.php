<x-app-layout>
<div class="container-fluid px-1"><div class="d-flex justify-content-between align-items-center mb-3"><h3>Ticket List</h3><a href="{{ route('support-center.create-ticket') }}" class="btn btn-primary">Add Ticket</a></div>@if(session('support_message'))<div class="alert alert-success">{{ session('support_message') }}</div>@endif
<div class="card shadow-sm border-0"><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-2"><select name="status" class="form-select"><option value="">All Status</option>@foreach(['new','open','pending','in_progress','resolved','closed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst(str_replace('_',' ',$s)) }}</option>@endforeach</select></div><div class="col-md-2"><select name="ticket_type" class="form-select"><option value="">All Types</option>@foreach(['complain','task','sales'] as $t)<option value="{{ $t }}" @selected(request('ticket_type')===$t)>{{ ucfirst($t) }}</option>@endforeach</select></div><div class="col-md-2"><select name="assigned_to" class="form-select"><option value="">All Staff</option>@foreach($staff as $person)<option value="{{ $person->id }}" @selected(request('assigned_to')==$person->id)>{{ $person->name }}</option>@endforeach</select></div><div class="col-md-4"><input name="q" value="{{ request('q') }}" class="form-control" placeholder="Ticket, CID, customer"></div><div class="col-md-2"><button class="btn btn-outline-primary w-100">Apply</button></div></form>
<div class="table-responsive"><table class="table align-middle"><thead><tr><th>#</th><th>Customer</th><th>Type / Topic</th><th>Subject</th><th>Priority</th><th>Assigned</th><th>Status</th><th>Updated</th></tr></thead><tbody>@forelse($tickets as $t)<tr><td class="fw-bold text-primary">#{{ $t->ticket_no }}</td><td>{{ $t->customer->customer_name ?? $t->customer_unique_id }}<br><small>{{ $t->customer_unique_id }}</small></td><td>{{ ucfirst($t->ticket_type) }}<br><small>{{ $t->topic }}</small></td><td>{{ \Illuminate\Support\Str::limit($t->subject, 50) }}</td><td>{{ ucfirst($t->priority) }}</td><td>{{ $t->assignee->name ?? 'Unassigned' }}</td><td>{{ ucfirst(str_replace('_',' ',$t->status)) }}</td><td>{{ $t->updated_at?->format('d M H:i') }}</td></tr>@empty<tr><td colspan="8" class="text-center text-muted py-4">No tickets found.</td></tr>@endforelse</tbody></table></div>{{ $tickets->links() }}</div></div></div>
</x-app-layout>

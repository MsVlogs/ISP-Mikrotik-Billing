<x-app-layout>
<div class="container-fluid px-1"><div class="d-flex justify-content-between mb-3"><h3>KYC Update</h3><a href="{{ route('support-center') }}" class="btn btn-outline-secondary">Back to Support</a></div><div class="card shadow-sm border-0"><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-3"><select name="status" class="form-select"><option value="">All</option><option value="pending">Pending</option><option value="reviewed">Reviewed</option><option value="rejected">Rejected</option></select></div><div class="col-md-6"><input name="q" value="{{ request('q') }}" class="form-control" placeholder="Request, CID, customer, phone, NID or email"></div><div class="col-md-3"><button class="btn btn-outline-primary w-100">Apply</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Customer</th><th>Phone</th><th>NID</th><th>Document</th><th>Status</th><th>Reviewed</th></tr></thead><tbody>@forelse($requests as $r)<tr><td>{{ $r->customer_name ?: ($r->customer->customer_name ?? $r->customer_unique_id) }}<br><small>{{ $r->customer_unique_id }}</small></td><td>{{ $r->phone ?: ($r->customer->mobile ?? '—') }}</td><td>{{ $r->nid ?: '—' }}</td><td>{{ $r->document_type ?: '—' }} {{ $r->document_reference }}</td><td>{{ ucfirst($r->status) }}</td><td>{{ $r->reviewed_at?->format('d M H:i') ?: '—' }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">No KYC requests found.</td></tr>@endforelse</tbody></table></div>{{ $requests->links() }}</div></div></div>
</x-app-layout>

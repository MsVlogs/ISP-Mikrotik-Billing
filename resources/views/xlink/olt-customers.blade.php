@push('styles')<link rel="stylesheet" href="{{ asset('xlink-olt/olt-source.css') }}">@endpush
<div class="d-flex justify-content-between align-items-center mb-3"><div><span class="text-uppercase small text-muted fw-semibold">Admin Panel</span><h3 class="mb-0">OLT Customers / ONU</h3><small class="text-muted">{{ $device->name }} · {{ $device->ip_address ?: $device->host ?: 'No IP' }}</small></div><a class="btn btn-outline-secondary" href="{{ route('network-inventory.olt') }}">Back to OLT</a></div>
<div class="row g-3 mb-3"><div class="col-md-3"><div class="card"><div class="card-body"><small>ONU Total</small><h4>{{ $mappings->count() }}</h4></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><small>Online</small><h4 class="text-success">{{ $mappings->where('status','online')->count() }}</h4></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><small>Mapped Customers</small><h4>{{ $mappings->whereNotNull('customer_id')->count() }}</h4></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><small>Mapped PPPoE</small><h4>{{ $mappings->whereNotNull('ppp_user_id')->count() }}</h4></div></div></div></div>
<div class="card shadow-sm mb-3"><div class="card-body"><form method="GET" class="row g-2"><div class="col-md-7"><input class="form-control" name="q" value="{{ $q ?? '' }}" placeholder="Search customer, PPPoE, ONU ID, serial, MAC or PON"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All Status</option><option value="online" @selected(($status ?? '')==='online')>Online</option><option value="offline" @selected(($status ?? '')==='offline')>Offline</option><option value="unknown" @selected(($status ?? '')==='unknown')>Unknown</option></select></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-primary flex-fill">Search</button><a class="btn btn-outline-secondary" href="{{ route('network-inventory.olt.customers',$device) }}">Reset</a></div></form></div></div>
<div class="card shadow-sm"><div class="card-header d-flex justify-content-between align-items-center"><strong>ONU / Customer Mappings</strong><span class="text-muted small">{{ $mappings->count() }} result(s)</span></div><div class="card-body p-0"><div class="table-responsive"><table class="table mb-0 align-middle"><thead><tr><th>Customer</th><th>PPPoE</th><th>ONU ID</th><th>Serial / MAC</th><th>PON</th><th>Status</th><th>RX / TX</th><th>Last Seen</th></tr></thead><tbody>
@forelse($mappings as $m)<tr><td><strong>{{ optional($m->customer)->customer_name ?: optional($m->customer)->customer_unique_id ?: 'Unmapped' }}</strong><small class="d-block text-muted">{{ optional($m->customer)->mobile }}</small></td><td>{{ optional($m->pppUser)->username ?: '—' }}</td><td>{{ $m->onu_id ?: '—' }}</td><td>{{ $m->onu_serial ?: '—' }}<small class="d-block text-muted">{{ $m->onu_mac ?: '' }}</small></td><td>{{ $m->pon_port ?: '—' }}</td><td><span class="badge {{ $m->status==='online'?'bg-success':($m->status==='offline'?'bg-danger':'bg-secondary') }}">{{ ucfirst($m->status) }}</span></td><td>{{ $m->rx_power !== null ? $m->rx_power.' dBm' : '—' }} / {{ $m->tx_power !== null ? $m->tx_power.' dBm' : '—' }}</td><td>{{ optional($m->last_seen_at)->format('d M Y H:i:s') ?: '—' }}</td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-muted">No real ONU/customer mappings exist yet. Telemetry can populate this table when configured.</td></tr>@endforelse
</tbody></table></div></div></div>
